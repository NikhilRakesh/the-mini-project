<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/dist/output.css" rel="stylesheet">
    <title>Document</title>
</head>
<body class="bg-gray-900 text-white p-8 md:p-16">
    <main class="bg-gray-900 text-white p-8 md:p-16">
        <form action="/updateuser/<%= user._id %>" method="POST" class="bg-gray-800 p-4 md:p-8 rounded-lg shadow-lg">
            <div class="mb-4">
                <label for="username" class="block text-gray-300">Username:</label>
                <input type="text" name="username" value="<%= user.username %>" class="bg-gray-700 border border-gray-600 text-white rounded-md px-3 py-2 w-full" required>
            </div>
    
            <div class="mb-4">
                <label for="email" class="block text-gray-300">Email:</label>
                <input type="email" name="email" value="<%= user.email %>" class="bg-gray-700 border border-gray-600 text-white rounded-md px-3 py-2 w-full" required>
            </div>
    
            <!-- Add more input fields for other user attributes as needed -->
    
            <div class="mt-6">
                <button type="submit" class="bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 text-white font-medium rounded-lg text-sm px-5 py-2.5 text-center dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800 w-full md:w-auto">Save Changes</button>
            </div>
        </form>
    </main>
    
</body>
</html>